// DMux8Way.hdl
// sel[2] parte en dos mitades y cada mitad se resuelve con un DMux4Way.

CHIP DMux8Way {
    IN in, sel[3];
    OUT a, b, c, d, e, f, g, h;

    PARTS:
    DMux (in=in, sel=sel[2], a=toABCD, b=toEFGH);
    DMux4Way (in=toABCD, sel=sel[0..1], a=a, b=b, c=c, d=d);
    DMux4Way (in=toEFGH, sel=sel[0..1], a=e, b=f, c=g, d=h);
}

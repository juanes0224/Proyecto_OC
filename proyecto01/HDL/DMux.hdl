// DMux.hdl
// Reparte la entrada hacia una de dos salidas; la otra queda en 0.
// No se requiere Or porque no se fusionan senales, se distribuyen.

CHIP DMux {
    IN in, sel;
    OUT a, b;

    PARTS:
    Not (in=sel, out=notSel);
    And (a=in, b=notSel, out=a);
    And (a=in, b=sel, out=b);
}

// Inc16.hdl
// Incrementa en 1 un numero de 16 bits.
// Es una suma con la constante 1: se pone el bit 0 en true
// y el resto del segundo operando en false.

CHIP Inc16 {
    IN in[16];
    OUT out[16];

    PARTS:
    Add16 (a=in, b[0]=true, b[1..15]=false, out=out);
}
